import { StartChoice } from '@m544/ui/requests/start/StartChoice';

/** /requests/start — the choice between the manual wizard and the chat assistant. */
export default function StartRequestPage() {
  return <StartChoice />;
}
